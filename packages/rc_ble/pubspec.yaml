name: rc_ble
description: BLE transport, protocol and logging core for RC configurator apps.
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.30.0
  flutter_riverpod: ^2.5.1
  sqflite: ^2.4.1
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
