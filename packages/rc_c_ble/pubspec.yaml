name: rc_c_ble
description: Receiver-specific BLE protocol and session layer for controller apps.
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.30.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

